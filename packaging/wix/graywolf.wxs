<Wix xmlns="http://wixtoolset.org/schemas/v4/wxs">
  <Package Name="Graywolf"
           Manufacturer="chrissnell"
           Version="$(var.Version)"
           UpgradeCode="c3a7f1d9-5e2b-4c8a-9f0d-b6e4a8c2d0f3"
           Scope="perMachine"
           Compressed="yes">

    <MajorUpgrade DowngradeErrorMessage="A newer version of Graywolf is already installed." />
    <MediaTemplate EmbedCab="yes" />

    <Feature Id="Main" Title="Graywolf" Level="1">
      <ComponentGroupRef Id="DataDirectory" />
      <ComponentGroupRef Id="ProductComponents" />
      <ComponentGroupRef Id="ModemComponents" />
    </Feature>
  </Package>

  <!-- Install directory (binaries) -->
  <Fragment>
    <StandardDirectory Id="ProgramFiles6432Folder">
      <Directory Id="INSTALLFOLDER" Name="Graywolf" />
    </StandardDirectory>
  </Fragment>

  <!-- Data directory (%ProgramData%\Graywolf) for writable files -->
  <Fragment>
    <StandardDirectory Id="CommonAppDataFolder">
      <Directory Id="DATADIR" Name="Graywolf" />
    </StandardDirectory>
  </Fragment>

  <!-- Create the data directory so the service can write its DB -->
  <Fragment>
    <ComponentGroup Id="DataDirectory" Directory="DATADIR">
      <Component Id="DataDirCreate" Guid="a1b2c3d4-5e6f-7a8b-9c0d-e1f2a3b4c5d6">
        <CreateFolder />
      </Component>
    </ComponentGroup>
  </Fragment>

  <!-- Main binary + Windows service -->
  <Fragment>
    <ComponentGroup Id="ProductComponents" Directory="INSTALLFOLDER">
      <Component Id="MainExecutable" Guid="d4f6e8a0-1b2c-3d4e-5f6a-7b8c9d0e1f2a">
        <File Id="graywolf.exe" Source="$(var.BinaryPath)" KeyPath="yes" />
        <ServiceInstall Id="GraywolfService"
                        Name="graywolf"
                        DisplayName="Graywolf APRS"
                        Description="APRS radio transceiver and gateway"
                        Start="demand"
                        Type="ownProcess"
                        ErrorControl="normal"
                        Arguments="-config &quot;[DATADIR]graywolf.db&quot; -modem &quot;[INSTALLFOLDER]graywolf-modem.exe&quot; -http 127.0.0.1:8080" />
        <ServiceControl Id="GraywolfServiceControl"
                        Name="graywolf"
                        Stop="both"
                        Remove="uninstall"
                        Wait="yes" />
      </Component>
    </ComponentGroup>
  </Fragment>

  <!-- Modem binary -->
  <Fragment>
    <ComponentGroup Id="ModemComponents" Directory="INSTALLFOLDER">
      <Component Id="ModemExecutable" Guid="e5a7b9c1-2d3e-4f5a-6b7c-8d9e0f1a2b3c">
        <File Id="graywolf_modem.exe" Source="$(var.ModemPath)" KeyPath="yes" />
      </Component>
    </ComponentGroup>
  </Fragment>
</Wix>
